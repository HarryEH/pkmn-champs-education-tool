import React from 'react';

/** Flow B — Detection + analysis dashboard. Phase-0 placeholder; WS-E owns this. */
export function DetectionScreen() {
  return (
    <div style={{ padding: 'var(--space-6)' }}>
      <h1 style={{ marginTop: 0 }}>Detection &amp; Analysis</h1>
      <p style={{ color: 'var(--text-mut)' }}>
        Capture the opponent&apos;s team preview, then analyse matchups, speed tiers, damage, and
        common sets. (WS-E)
      </p>
    </div>
  );
}
